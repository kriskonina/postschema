version: '3'
services:
  redis:
    image: redis
    ports:
      - "6379:6379"
  postgres:
    image: postgres:10
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASS=1234
      - POSTGRES_DB=postschemadb
      - POSTGRES_USER=postschema
      - ALLOW_IP_RANGE=0.0.0.0/0
